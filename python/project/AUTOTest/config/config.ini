#  this is config file, only store browser type and server URL
[browserType]
;browserName = Firefox
browserName = Chrome
;browserName = IE

[testServer]
;URL = http://XXXXXXXXXXXXXXX  #输入你要测得url地址
URL = 'http://www.hualongxiang.com/'



;
;; 自动化项目配置文件
;
;[Project]
;; 项目基础配置
;name = My Automation Project
;env = dev ; 环境类型 (dev/test/prod)
;log_level = DEBUG ; 日志级别 (DEBUG/INFO/WARNING/ERROR)
;
;[Paths]
;; 路径配置
;input_dir = ./data/input
;output_dir = ./data/output
;log_dir = ./logs
;
;[Database]
;; 数据库配置
;host = localhost
;port = 3306
;user = automation_user
;password = s3cr3tP@ss
;database = automation_db
;auto_reconnect = True ; 是否自动重连
;
;[WebDriver]
;; 浏览器驱动配置
;browser = chrome ; 可选 chrome/firefox/edge
;headless = True ; 是否无头模式
;timeout = 30 ; 元素查找超时时间 (秒)
;download_dir = ${Paths:output_dir}/downloads ; 引用其他 section 的值
;
;[APIs]
;; API 服务配置
;base_url = https://api.example.com/v1
;retry_times = 3
;api_key = ABCDEF123456
;timeout = 10.5 ; 请求超时时间 (秒)
;
;[Email]
;; 邮件通知配置
;enabled = True
;smtp_server = smtp.example.com
;smtp_port = 587
;sender = automation@example.com
;recipients = admin@example.com, dev@example.com
;use_ssl = True